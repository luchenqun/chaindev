# 前端实现约定

## 组件选型

- 下拉菜单、选择器、下拉框、Combobox 这类交互组件，优先使用 shadcn 体系组件或基于 shadcn 封装。
- 非必要情况下，不要再手写原生 `select` 弹层样式来替代 shadcn。
- 如果现有页面已经用了非 shadcn 实现，后续迭代时优先收敛到 shadcn 风格和交互行为。

## 目的

- 保持交互一致性。
- 减少浏览器原生下拉样式差异。
- 降低后续主题化、可访问性和样式维护成本。
